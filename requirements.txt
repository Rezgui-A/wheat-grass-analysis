Flask==2.3.3
Werkzeug==2.3.7
numpy==1.24.3
opencv-python-headless==4.8.1.78
matplotlib==3.7.2
scipy==1.11.2
Pillow==10.0.0
gunicorn==21.2.0

# Use CPU-only PyTorch for Render (much smaller, faster to install)
torch==2.0.1+cpu --index-url https://download.pytorch.org/whl/cpu
torchvision==0.15.2+cpu --index-url https://download.pytorch.org/whl/cpu

# SAM - use the PyPI version (not GitHub)
segment-anything-py==1.0.1

# Other dependencies that might help
onnxruntime==1.15.1